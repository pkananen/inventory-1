
<h4>Your Details</h4>
<%= devise_error_messages! %>
<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  

  <div class="email" style="clear:both"><%= f.label :email %><br />
  <%= f.email_field :email, :autocomplete => :off %></div>

  <div class="signin"><%= f.label :password %> (min. 6 characters)<br />
  <%= f.password_field :password, :autocomplete => :off %></div>

  <div><%= f.label :password_confirmation %><br />
  <%= f.password_field :password_confirmation, :autocomplete => :off %></div>

  <div class="signin"><%= f.submit "sign up", :class => 'button new sign_up' %></div>
<% end %>

<%#= render "devise/shared/links" %>
</div>